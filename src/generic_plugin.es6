w.AF = w.AF || {}

w.AF.GenericPlugin = class {
  constructor(key, config){
    this.key = key
    this.config = config
  }

  // shuts down this object by disabling the widget and country selector
  destroy(){

  }
}
